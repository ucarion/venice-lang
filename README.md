```
// Comments like this
pub struct Person {
    pub name: String,
    birth_date: DateTime,
    favorite_color: Color
}

enum Color {
    White,
    Blue,
    Red,
    Purple
}

def main() {
    nums = 0.upto(10)
    evens = nums.filter(fn n: { n % 2 == 0 })
}
```
